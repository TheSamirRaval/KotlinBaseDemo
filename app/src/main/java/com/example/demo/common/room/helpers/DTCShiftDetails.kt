package com.example.demo.common.room.helpers

/**
 * This class is used to convert DataType for List Of Model Class
 */
open class DTCShiftDetails {

/*
    private val gson = Gson()
    @TypeConverter
      fun stringToList(data: String?): List<Shifts> {
        if (data == null) {
            return emptyList()
        }

        val listType = object : TypeToken<List<Shifts>>() {

        }.type

        return gson.fromJson(data, listType)
    }

    @TypeConverter
    fun ListToString(someObjects: List<Shifts>): String {
        return gson.toJson(someObjects)
    }*/
}